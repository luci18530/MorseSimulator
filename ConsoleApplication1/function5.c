#include <stdio.h>
#include <windows.h>
#include <dos.h>
#include <stdlib.h>
#include <locale.h>
#include <string.h>
#include "codigos.h"
#include "function5.h"

char* parse(char *phrase)
{
	char *words[9999964], ch, wph[50];
	int count1 = 0, count2;
	while (ch != '\0' && ch != EOF)
	{
		while (ch != ' ', ch != '|')
		{
			count
		}
	}



}